# Ignore directories generated by Composer
/drush/contrib/
/vendor/
vendor/
/web/core/
/web/modules/contrib/
/web/themes/contrib/
/web/profiles/contrib/
/web/libraries/

# Ignore Drupal's file directory
/web/sites/*/files/

# Ingore files that are only for the current local environment
web/sites/*/settings.local.php
web/sites/*/services.local.yml

# Ignore SimpleTest multi-site environment.
web/sites/simpletest

# behat screenshots
tests/behat/screenshots

# This is because of a bug in drupal-console, can remove when composer install no long creates this directory.
console

# Site audit needs to live inside the drupal to run with @self.
audit-site
