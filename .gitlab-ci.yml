# This file is locked for GovCMS SaaS.
include:

  # Starting inputs for the GitLab CI configuration.
  - local: '/.gitlab-ci-inputs.yml'

  # The primary default GovCMS GitLab CI configuration.
#  - project: 'govcms/image-tooling'
#    ref: master
#    file: 'gitlab/.gitlab-ci-main.yml'

  # Replaces the above until something like https://github.com/govcms/scaffold-tooling versioned and in Gitlab.
  - remote: 'https://raw.githubusercontent.com/simesy/scaffold-tooling/ff7e1cbd3be7ef8158c7b186386be878c62fabcc/gitlab/.gitlab-ci-main.yml'
